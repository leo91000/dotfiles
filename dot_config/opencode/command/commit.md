---
description: Generate commit message
---

Based on the following changes, generate a conventional commit message:

!`git diff --staged`

Follow the project's commit conventions:

- Use format: "type(scope): message"
- Types: fix, feat, chore, docs, refactor, test
- Include JIRA ticket reference if applicable
- Keep it concise and descriptive
